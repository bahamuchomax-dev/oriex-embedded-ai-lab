// Types for '@huggingface/transformers' are now provided by the installed
// package itself, so no ambient module declaration is needed here. This file is
// intentionally left without declarations to avoid conflicting with the real
// package types.
export {}
